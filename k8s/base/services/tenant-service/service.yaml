apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: opsnexus
  labels:
    app: tenant-service
    component: service
spec:
  type: ClusterIP
  selector:
    app: tenant-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
