apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: opsnexus
  labels:
    app: workflow-service
    component: service
spec:
  type: ClusterIP
  selector:
    app: workflow-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
