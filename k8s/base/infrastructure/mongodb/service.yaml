apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: opsnexus
  labels:
    app: mongodb
    component: infrastructure
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017
      protocol: TCP
