apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: docdb-secret
  namespace: opsnexus
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: docdb-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: opsnexus/${ENVIRONMENT}/docdb
